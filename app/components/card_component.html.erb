<div class="bg-base02 overflow-hidden shadow rounded-lg divide-y divide-base00">
  <% if header %>
    <div class="px-4 py-5 sm:px-6">
      <h3 class="text-lg leading-6 font-medium text-gray-900">
        <%= header %>
      </h3>
    </div>
  <% end %>

  <div class="px-4 py-5 sm:p-6">
    <%= content %>
  </div>

  <% if footer %>
    <div class="px-4 py-4 sm:px-6">
      <%= footer %>
    </div>
  <% end %>
</div>